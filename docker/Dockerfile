# Используем официальный образ Node.js последней LTS версии
FROM node:20-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Устанавливаем глобально необходимые инструменты
RUN npm install -g npm@latest nuxi@latest

# Разрешаем использование портов для dev-сервера
EXPOSE 3000 24678